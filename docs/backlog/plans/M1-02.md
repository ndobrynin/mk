# Plan M1-02

## Verdict
valid

## Touches
- `apps/api/package.json` — change (add `bcryptjs` + `@types/bcryptjs`)
- `pnpm-lock.yaml` — change (lockfile update)
- `apps/api/src/prisma/prisma.service.ts` — create (PrismaClient, onModuleInit/onModuleDestroy)
- `apps/api/src/prisma/prisma.module.ts` — create (`@Global`, exports PrismaService)
- `apps/api/src/auth/auth.controller.ts` — create (`POST /auth/register`)
- `apps/api/src/auth/auth.service.ts` — create (validate password ≥8, bcryptjs hash, create User, 409 on unique email)
- `apps/api/src/auth/auth.module.ts` — create (wires controller+service, imports PrismaModule)
- `apps/api/src/app.module.ts` — change (import AuthModule + PrismaModule)
- `apps/api/test/auth-register.e2e.spec.ts` — create (supertest, ≥3 tests)

## Steps
as in ticket

## Tests
- `pnpm --filter @kidagrad/api test` — exit 0, passed ≥ 3

## Out of scope
- login, cookies, OAuth
- JWT/sessions (M1-03/04)
- curl as a gate
