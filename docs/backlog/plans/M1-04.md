# Plan M1-04

## Verdict
valid

## Touches
- apps/api/prisma/schema.prisma — change: add `RefreshToken { id, userId, tokenHash @unique, expiresAt, createdAt }` + relation to User
- apps/api/prisma/migrations/<ts>_add_refresh_tokens/ — create: via `prisma migrate dev`
- apps/api/src/auth/auth.service.ts — change: opaque refresh token (crypto.randomBytes, sha256 hash stored); login/register return `{ accessToken, refreshToken }` (register keeps `id, email` too); add `refresh(token)`: hash lookup, expired/not-found → 401, else delete used row + issue new pair (rotation)
- apps/api/src/auth/auth.controller.ts — change: add `POST /auth/refresh { refreshToken } → { accessToken, refreshToken }`
- apps/api/test/auth-refresh.e2e.spec.ts — create: rotation issues new access; reused old refresh → 401 (min_tests 2)
- apps/api/test/auth-login.e2e.spec.ts — change: assert `refreshToken` present alongside `accessToken`
- pnpm-lock.yaml — change (only if `prisma migrate dev` touches it)

Not touched: apps/api/test/auth-register.e2e.spec.ts (uses `toMatchObject`/`not.toHaveProperty`, tolerates added token fields), apps/api/package.json (no new deps — `node:crypto` is built-in).

## Steps
- Prisma model + migration for `refresh_tokens`
- Service: shared token-pair issuance helper used by register/login
- Service: `refresh()` with hash lookup + delete-on-rotate (absent row = 401, covers reuse)
- Controller: wire `POST /auth/refresh`
- Tests: new refresh spec + login spec assertion

## Tests
- `pnpm --filter @kidagrad/api test` — exit 0, passed ≥ 2 (rotation + reuse 401)

## Out of scope
- logout all devices, OAuth, curl
