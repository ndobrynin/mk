# Plan M0-05

## Verdict
valid

## Touches
- `packages/shared/package.json` — create
- `packages/shared/tsconfig.json` — create
- `packages/shared/src/index.ts` — create

## Steps
as in ticket

## Tests
- `pnpm install` — exit 0
- `node -e "const p=require('./packages/shared/package.json'); if(p.name!=='@kidagrad/shared') process.exit(1)"` — exit 0
- `grep -q 'protocolVersion' packages/shared/src/index.ts` — exit 0

## Out of scope
- Zod-схемы комнат, карт, сокетов
