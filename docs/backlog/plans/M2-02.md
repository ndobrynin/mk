# Plan M2-02

## Verdict
valid

## Touches
- packages/engine/src/index.ts — change
- packages/engine/src/index.test.ts — change

## Steps
- Change `setup(playerIds: [string, string])` → `setup(playerIds: string[]): GameState`
- Throw (`Error`, not `apply`-result) if `playerIds.length` is not 2, 3, or 4
- Keep per-player init unchanged: `id`, `coins: 3`, `establishments: ['wheat-field','bakery']`, 9 `landmarks` unconstructed, `phase: 'rolling'`, `activeIndex: 0`
- Preserve order: `players[0]` is first active

## Tests
- `pnpm --filter @kidagrad/engine test` — exit 0, passed ≥ 1
- Update existing `setup(['p1','p2'])` calls to still pass (2-player case)
- Add: setup with 3 ids → 3 players, same per-player shape
- Add: setup with 4 ids → 4 players, same per-player shape
- Add: setup with 0, 1, or 5 ids each throws
- Keep existing `apply` stub tests untouched

## Out of scope
- `roll` / income logic
- Changing `apply` signature or `CONTRACT.md`
