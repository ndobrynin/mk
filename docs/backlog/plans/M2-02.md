# Plan M2-02

## Verdict
valid

## Touches
- `packages/engine/src/index.ts` — change
- `packages/engine/src/index.test.ts` — change

## Steps
- Add `Player` shape (coins, establishment ids, 9 landmarks with `constructed`) as internal type; keep `GameState.players` structurally compatible (no CONTRACT.md edit)
- Add `activeIndex` to `GameState` construction only via a typed builder (avoid excess-property errors on the frozen literal type); do not alter `apply`'s params/return type
- Add stable landmark id list as internal const: `harbor, station, mall, tv-tower, amusement-park, aqua-park, airport, bank, city-hall`
- Implement `setup(playerIds: [string, string]): GameState`: `version: 1`, `phase: 'rolling'`, `activeIndex: 0`, each player `coins: 3`, establishments `['wheat-field','bakery']`, all 9 landmarks `constructed: false`
- No roll/income logic; no RNG usage
- Do not modify `packages/engine/CONTRACT.md` or the `apply` signature

## Tests
- `pnpm --filter @kidagrad/engine test` — exit 0, passed ≥ 1 (assert setup output shape, no RNG dependency)

## Out of scope
- roll, доход, любые изменения `apply`
