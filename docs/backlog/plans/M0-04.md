# Plan M0-04

## Verdict
valid

## Touches
- `.env.example` — create

## Steps
as in ticket

## Tests
- `grep -q '5435' .env.example && grep -q '6380' .env.example && grep -q '4010' .env.example` — exit 0
- `! grep -E '5432|6379' .env.example` — exit 0

## Out of scope
- `.env` with secrets in git
