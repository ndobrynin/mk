# Plan M5-02

## Verdict
valid

## Touches
- apps/api/src/rooms/rooms.service.ts — change
- apps/api/src/rooms/rooms.controller.ts — change
- apps/api/src/game/game.service.ts — change
- apps/api/src/game/game.gateway.ts — change
- apps/api/src/game/game.types.ts — change
- apps/api/test/rooms.e2e.spec.ts — change
- apps/api/test/game-socket.e2e.spec.ts — change
- apps/web/src/pages/RoomLobbyPage.tsx — change
- apps/web/src/pages/RoomLobbyPage.test.tsx — change
- apps/web/src/pages/RoomsPage.tsx — change
- apps/web/src/lib/api.ts — change
- apps/web/src/i18n/ru.ts — change

## Steps
- Bot = User + Identity `provider: 'bot'` (no prisma migration); seat DTO `isBot`. Host-only `POST /rooms/:id/bots` (REST like join/leave, not a new socket event). `POST /rooms` optional `fillBots` fills empty slots at create.
- After ok `start`/`apply`: if active seat is bot and phase ≠ `gameOver`, delay 0.8–1.5s (`BOT_MOVE_DELAY_MS` override for tests), `chooseBotCommand` + existing `applyCommand`, emit `game.snapshot`/`game.events`, reschedule. Failed human apply does not schedule.
- Lobby: host add-bot button + bot badge (`ru`); merge REST seats into local/live state. RoomsPage fill-bots checkbox. Bots skip ready. Redis 6380 only. Do not change `apply` signature or `socket.ts`.

## Tests
- `pnpm --filter @kidagrad/api test` — exit 0, passed ≥ 3
- `pnpm --filter @kidagrad/web test` — exit 0
- `pnpm --filter @kidagrad/web build` — exit 0

## Out of scope
- Human turn timer / disconnect-to-bot (M7), VK/Yandex (M6), PixiJS, client dice/income, `apply` signature, prisma schema/migration
