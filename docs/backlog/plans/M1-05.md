# Plan M1-05

## Verdict
valid

## Touches
- apps/api/src/auth/jwt-auth.guard.ts — create
- apps/api/src/me/me.controller.ts — create
- apps/api/src/app.module.ts — change
- apps/api/test/me.e2e.spec.ts — create

## Steps
- `JwtAuthGuard` (CanActivate, no @nestjs/passport): read `Authorization: Bearer <token>`; missing/malformed header → `UnauthorizedException`
- Verify with `jwt.verify(token, process.env.JWT_SECRET)` (same `jsonwebtoken` lib as `auth.service.ts`); throw on invalid/expired
- On success attach `{ id: payload.sub, email: payload.email }` to `req.user`; no Prisma lookup needed
- `MeController`: `@Get('me')` + `@UseGuards(JwtAuthGuard)`, returns `req.user`
- Register `MeController` in `AppModule.controllers`
- e2e: register+login → 200 `{id,email}` with Bearer access; 401 no header; 401 garbage token

## Tests
- `pnpm --filter @kidagrad/api test` — exit 0, passed ≥ 2

## Out of scope
- profile, avatar, VK linking
- curl-based verification
