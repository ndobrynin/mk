# Plan M1-03

## Verdict
valid

## Touches
- `apps/api/package.json` — change (add `jsonwebtoken` + `@types/jsonwebtoken`)
- `pnpm-lock.yaml` — change (lockfile update)
- `apps/api/src/auth/auth.service.ts` — change (add `login(email, password)`: find user, bcrypt compare, sign JWT via `jsonwebtoken` with `process.env.JWT_SECRET`, short TTL e.g. `15m`; same 401 for missing user and wrong password, no leak)
- `apps/api/src/auth/auth.controller.ts` — change (add `POST /auth/login`, 200 `{ accessToken }`)
- `apps/api/test/auth-login.e2e.spec.ts` — create (supertest, ≥2 tests: register+login → `accessToken` with 2 dots; wrong password → 401)

## Steps
as in ticket

## Tests
- `pnpm --filter @kidagrad/api test` — exit 0, passed ≥ 2, login ok + 401 scenarios

## Out of scope
- refresh token / rotation (M1-04)
- `/me`
- curl as a gate
