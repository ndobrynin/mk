# Plan M4-01

## Verdict
valid

## Blockers
M1-05: done. M3-01: done.

## Touches
- `apps/web/package.json` — change (react-router-dom, vitest, @testing-library/react, jsdom, etc.)
- `apps/web/vite.config.ts` — change (test config for vitest, keep port 5173)
- `apps/web/src/` — change/create: router, pages (login/register, rooms list+create, room lobby), словарь `ru`, api client (`VITE_API_URL`)
- `apps/web/src/**/*.test.tsx` — create (mock fetch, no live API)
- `apps/api/src/main.ts` — change: `app.enableCors` origin `http://127.0.0.1:5173` (+ `http://localhost:5173`)
- `.github/workflows/test.yml` — change: add `pnpm --filter @kidagrad/web test` step before `Build @kidagrad/web`
- `pnpm-lock.yaml` — change (lockfile)

## Steps
as in ticket

## Tests
- `pnpm --filter @kidagrad/web test` — exit 0, passed ≥ 3
- `pnpm --filter @kidagrad/web build` — exit 0

## Out of scope
Socket.IO, стол, магазин, боты, VK/Яндекс, портрет, PixiJS, ready/start (M4-02).
