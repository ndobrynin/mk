# Plan M3-01

## Verdict
valid

## Touches
- apps/api/prisma/schema.prisma — change (models Room, RoomSeat)
- apps/api/prisma/migrations/ — create migration (5435, DATABASE_URL from env)
- apps/api/src/app.module.ts — change (import RoomsModule)
- apps/api/src/rooms/ — create (rooms.module.ts, rooms.controller.ts, rooms.service.ts)
- apps/api/test/rooms.e2e.spec.ts — create

## Steps
as in ticket

## Tests
- `pnpm --filter @kidagrad/api test` — exit 0, passed ≥ 4

Cover (supertest, pattern like `test/me.e2e.spec.ts`: register+login for real JWT):
- POST /rooms → 201, host in seat 0, room appears in GET /rooms if isPublic
- POST /rooms/:id/join and POST /rooms/join by code → second player seated
- join when maxSeats=4 full → 409; no Bearer → 401
- leave: pick and assert one fixed outcome (finished or deleted) for host-leaves-waiting-room

## Out of scope
Socket.IO, `start`, bots, Redis, engine, UI.
