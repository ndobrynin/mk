# Plan M2-04

## Verdict
valid

## Touches
- packages/engine/src/index.ts — change
- packages/engine/src/index.test.ts — change
- packages/engine/test/fixtures/cafe-one-coin.json — create
- packages/engine/test/fixtures/cafe-zero-coins.json — create

## Steps
as in ticket

## Tests
- `pnpm --filter @kidagrad/engine test` — exit 0, passed ≥ 7
- update existing M2-03 test asserting `phase === 'income'` to `'build'`
- add: rng 0 → both 3→4 (wheat-field)
- add: rng 1 → active 3→4, opponent 3 (bakery)
- add: rng 3 → both stay 3
- add: two cafe tests reading JSON fixtures (cafe-one-coin, cafe-zero-coins), debt beyond coins burns
- add: setup → roll → passBuild → activeIndex 0→1→0, turn goes to player 1
- add: passBuild from `rolling` → `{ ok: false }`

## Out of scope
- other cards, Торговый центр, buildEstablishment, 2–3 dice, rerolls, port
- apply signature / CONTRACT.md changes
- ticket/README/M2-05/M2-06 file edits (parent scope)
