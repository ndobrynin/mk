# Plan M3-02

## Verdict
valid

## Touches
- `apps/api/src/game/` — create: GameGateway (Socket.IO), GameModule, in-memory/Redis room-lock
- `apps/api/src/redis/` — create: RedisModule/service, client on `REDIS_URL` (6380, not 6379)
- `apps/api/src/app.module.ts` — change: import GameModule
- `apps/api/src/main.ts` — change: only if needed to attach Socket.IO adapter to same HTTP server (port 4010)
- `apps/api/package.json` — change: add `socket.io`, redis client, `@kidagrad/engine`, `@kidagrad/shared` deps
- `apps/api/test/` — create: in-process Nest + `socket.io-client` tests, no long `listen`+`curl`
- `.github/workflows/test.yml` — change: add Redis service on 6380 + `REDIS_URL` env
- `packages/shared/src/index.ts` — change: Zod schemas for commands/events, only if needed
- `pnpm-lock.yaml` — lockfile

## Steps
as in ticket

## Tests
- `pnpm --filter @kidagrad/api test` — exit 0, passed ≥ 3

## Out of scope
- ботов и политику engine, таймеры хода, UI, OAuth
- смену `apply` signature / `packages/engine/CONTRACT.md`
- порты 6379 / 3000 / 5432 в compose/CI/коде api
