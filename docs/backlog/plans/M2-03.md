# Plan M2-03

## Verdict
valid

## Touches
- packages/engine/src/index.ts — change
- packages/engine/src/index.test.ts — change

## Steps
- `apply`: when `command.type === 'roll'` and `state.phase === 'rolling'`: `n = rng.nextInt(6) + 1` (ignore any extra `value` field on command)
- Return new state with `lastRoll: { dice: [n] }`, `phase: 'income'`; events `[{ type: 'diceRolled', dice: [n] }]`; `ok: true`
- Extend `GameState` locally with optional `lastRoll?: { dice: number[] }` (inline/intersection type, like existing `activeIndex`) — no `CONTRACT.md` edit, `apply` signature unchanged
- Any other phase/command (e.g. `passBuild`) keeps existing `{ ok: false, error: 'not implemented' }` fallback
- Test: fake `rng = { nextInt: () => 0 }` on `setup(['a','b'])` state; call `apply(state, { type: 'roll', value: 6 } as Command, rng)` → assert `events[0].dice === [1]`, `result.state.lastRoll.dice[0] === 1`, `phase === 'income'` (value ignored)

## Tests
- `pnpm --filter @kidagrad/engine test` — exit 0, passed ≥ 1

## Out of scope
- 2–3 dice, reroll (amusement park), harbor +2, income payout itself (M2-04), any port
