# Plan M0-03

## Verdict
valid

## Touches
- infra/docker-compose.yml — create
- infra/.dockerignore — create (optional)

## Steps
as in ticket

## Tests
- `docker compose -f infra/docker-compose.yml config` — exit 0, output shows 5435 and 6380, no 5432/5433/5434/6379 host publish
- `docker compose -f infra/docker-compose.yml up -d` (if Docker available) — both services healthy/up
- `docker compose -f infra/docker-compose.yml ps` — containers named `kidagrad-*`
- `lsof -nP -iTCP:5435,6380 -sTCP:LISTEN` — docker listening on these ports
- if `up` unavailable in agent env: config + port grep is enough; tester reports BLOCKED on up, not PASS

## Out of scope
- API application in compose
- pgAdmin
- ports 5432 / 5433 / 5434 / 6379
