# Plan M5-01

## Verdict
valid

## Touches
- packages/engine/src/bot.ts — create
- packages/engine/src/bot.test.ts — create
- packages/engine/src/index.ts — change
- packages/engine/CONTRACT.md — change

## Steps
- Add `chooseBotCommand(state, playerId): Command` (optional `legalCommands` via probing `apply` + stub rng). Re-export from `index.ts` (file end; `bot.ts` imports `apply`/types from `./index.js`). Do not change `apply` signature or frozen CONTRACT apply block.
- `rolling` → `{ type: 'roll' }` only (never `chooseDiceCount`). `decideReroll` → `keepRoll`. `decideHarbor` → `harborSkip`. pick* / `endOfTurn` → first legal.
- `build`: landmark if affordable (`cost !== null`) and city feeds (`city-hall` built or `establishments.length > 2`); else max EV/cost market establishment (1d6 activations, skip `cost: null`); else `passBuild`.
- CONTRACT.md: add Bot policy section only (`chooseBotCommand`, v1 rules above).
- `bot.test.ts`: ≥3 tests for AC — apply accepts chosen command; rolling picks `roll`; build with no affordable buy → `passBuild`.

## Tests
- `pnpm --filter @kidagrad/engine test` — exit 0, passed ≥ 3

## Out of scope
- Nest/React, sockets, delay scheduler, turn timers (M7), lobby UI, OAuth, filling room slots
