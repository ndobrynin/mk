# Plan M1-01

## Verdict
valid

## Touches
- apps/api/package.json — change (add `prisma`, `@prisma/client` deps; `generate`/`migrate` scripts)
- apps/api/prisma/schema.prisma — create
- apps/api/prisma/migrations/**_init/migration.sql — create (first migration, via `prisma migrate dev`)
- apps/api/prisma/migrations/migration_lock.toml — create (auto-generated by prisma)
- apps/api/prisma.config.ts — create only if Prisma version requires it instead of `package.json#prisma`
- pnpm-lock.yaml — change (lockfile)

## Steps
as in ticket

## Tests
- `pnpm --filter @kidagrad/api exec prisma validate` — exit 0
- `pnpm --filter @kidagrad/api exec prisma migrate deploy` — exit 0 (Postgres kidagrad up on 5435)

## Out of scope
- register/login эндпоинты
- seed пользователей
- подключение к 5432
