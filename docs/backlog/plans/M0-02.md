# Plan M0-02

## Verdict
valid

## Touches
- `package.json` — create
- `pnpm-workspace.yaml` — create
- `pnpm-lock.yaml` — появится после `pnpm install`

## Steps
as in ticket

## Tests
- `pnpm install` — exit 0
- `test -f pnpm-workspace.yaml && grep -q 'apps/\*' pnpm-workspace.yaml` — exit 0

## Out of scope
- приложения, зависимости Nest/React, Turborepo, Nx
- `npm` lockfile
